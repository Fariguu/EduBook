# Feature Slice: Auth

This slice handles all authentication operations for the professor.

## Structure
- `/actions`: login, logout, reset password, update password.
- `/components`: AuthModal, UpdatePasswordForm, LogoutButton.
- `/schemas`: Zod schemas for auth.
- `/utils`: Auth checks, permission utilities.
